[toolchain]
package_manager = "yarn"

[features]
resolution = true
skip-lint = false

[programs.localnet]
book = { address="B6FGFq5WVExospwD6XE4RiPdtjLp6R5wFRMYQeVPQsfz",path="../target/deploy/book.so"}

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
post_build = "mkdir -p ../shared/idls && cp target/idl/book.json ../shared/idls/"
test = "cargo test"

[hooks]
