/** 站点品牌静态资源（位于 `public/`） */
export const BOOKCHAIN_LOGO_SRC = '/BookChain_LOGO.png'

/** 无书籍封面 URL 时的占位图 */
export const BOOK_COVER_PLACEHOLDER_SRC = '/book-cover-placeholder.svg'

/** 用户未上传头像时前端兜底（后端 `DEFAULT_AVATAR_URL` 写入库后一般不再用到） */
export const DEFAULT_AVATAR_SRC = '/default-avatar.svg'
